# =============================================================================
# 盘古AI 系统依赖清单
# -----------------------------------------------------------------------------
# 安装方式：
#   完整安装（推荐）: pip install -r requirements.txt
#   仅核心功能:          pip install flask flask-cors requests numpy python-dotenv
#
# 国内用户建议加镜像：
#   pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
#   或设置环境变量后安装：
#   set PIP_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
#   set HF_ENDPOINT=https://hf-mirror.com
# =============================================================================

# =============================================================================
# 1. 核心依赖（必装 — 约 50MB）
# =============================================================================
# Web 框架（Java 后端也可，Python 后端用这个）
flask>=3.0
flask-cors>=4.0

# HTTP 请求（外部 API 调用、资源获取）
requests>=2.31

# 数值计算基础（贝叶斯/COX 都依赖）
numpy>=1.24

# 环境变量管理
python-dotenv>=1.0.0

# HTML 解析（抓取网页内容用）
beautifulsoup4>=4.12

# =============================================================================
# 2. 文本分析层（必装 — 分析引擎核心功能依赖，约 40MB）
# =============================================================================
# 中文分词（TDE 文本评分的词汇分析依赖）
jieba>=0.42.1

# 数据库 ORM（章节分析结果持久化）
sqlalchemy>=2.0.0

# =============================================================================
# 3. 统计分析层（建议装 — 贝叶斯+COX 依赖，约 200MB）
# -----------------------------------------------------------------------------
# 不装也能用 pangu.py / reference_engine.py 的基础功能
# 但 analyze 命令的统计分析报告会降级为简化版本
# =============================================================================
# 数据框（生存分析/贝叶斯计算依赖）
pandas>=2.0.0

# 统计检验（贝叶斯后验概率、显著性检验）
scipy>=1.10.0

# COX 生存分析引擎核心
lifelines>=0.28.0

# =============================================================================
# 4. 可选增强（按需安装）
# =============================================================================

# --- [LLM] 多模型统一调用 ---
# 装了可以通过统一 API 调用 100+ 大模型（OpenAI/DeepSeek/通义千问/豆包等）
# 不装则走原生 HTTP 请求，只支持简单 API 格式
litellm>=1.40

# --- [向量检索] 语义 RAG ---
# 装了可以用语义相似度检索参考资源（搜"伏笔"能匹配"悬念"相关内容）
# 不装则走关键词匹配（ReferenceEngine 自带）
faiss-cpu>=1.7
sentence-transformers>=2.7
# modelscope>=1.14          # 国内镜像替代 sentence-transformers

# --- [日志与用户体验] ---
# 结构化日志（替代 print）
loguru>=0.7.0
# 进度条
tqdm>=4.65.0
# rich>=13.0.0             # 终端彩色输出（当前代码未直接使用）

# --- [UI] Web 界面 ---
# gradio>=4.0.0             # 快速搭建 Web UI（当前代码未直接使用）
# python-docx>=1.1          # Word 文档导出（当前代码未直接使用）

# --- [实验性功能] ---
# chromadb>=0.4.0            # 更强大的向量数据库
# langchain>=0.1.0           # LLM 应用框架
